Proxy 类的讲解（它被应用于动态代理中，是动态代理的核心类）

	功能：
		运行期在内存中创建一个字节码文件（.class）
	一般要将一个类的字节码文件加载到内存的过程是：先编写一个java类  再编译生成class文件   最好将这个
			class文件通过类加载器   加载到内存   ；
			而Proxy类可以实现就是在运行期间动态生成一个字节码文件，并执行这个文件。如创建这个字节码的对象等
			，但是要求动态生成的字节码文件必须是某个接口的子类。