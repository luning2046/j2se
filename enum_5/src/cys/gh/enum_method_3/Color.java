package cys.gh.enum_method_3;

/*
 * 在枚举类型中每个元素都是此枚举类的一个实例
 * （枚举中的实例个数，就是在此枚举中定义多少个就有多少个此例中就三个实例）
 * 也就相当于一个类中的实例（类中的实例可以定义无数个）
 */
public enum Color {
	//实际此成员的修饰符是 public static final
	Red,White,Blue;
}
